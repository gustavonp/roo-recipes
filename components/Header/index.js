import * as React from 'react';
import { HeaderWrapper } from './lib/styled';

const Header = (props) => {
  
  return (
    <HeaderWrapper>
      <p>Hello</p>
    </HeaderWrapper>
  )
};

export default Header;